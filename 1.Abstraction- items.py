Class Item:
              "
              "
          @staticmethod

    def__connect(self, smpt_server):
                 pass

    def__prepare_body(self):
                return f""
                      Hello Someone.
                      We have (self.name} {self.quantity times.
                      Regards, JisshapedCoding
                      ""
    def __send(self):
                pass

    def send_email(self):
            self.__connect
            self.__prepare_body()
            self.__send()
